# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: fastmri_multicoil
  - override /model: varnet
  - override /callbacks: default
  - override /trainer: gpu

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["fastmri_brain", "varnet"]

seed: 42

trainer:
  min_epochs: 50
  max_epochs: 56
  gradient_clip_val: 0.5

ckpt_path: "/home/s2287251/idcom_imaging/Users/yuyang/VIOS-s/ReconUnlearning/logs/MRI_Recon_fastMRI_multicoil_mixed/runs/2024-01-11_16-30-11/checkpoints/epoch_049_step_1066450.ckpt"

# model:
#   optimizer:
#     lr: 0.001
#   net:
#     lin1_size: 128
#     lin2_size: 256
#     lin3_size: 64
#   compile: false

data:
  batch_size: 1

task_name: "MRI_Recon_fastMRI_multicoil_finetuning"


logger:
  comet:
    project_name: "fastMRI_unlearning_multicoil_finetuning"

# wandb:
#   tags: ${tags}
#   group: "mnist"
# aim:
#   experiment: "mnist"
